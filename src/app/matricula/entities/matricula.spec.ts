/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { Matricula } from './matricula';

describe('Matricula', () => {
  it('should create an instance', () => {
    expect(new Matricula()).toBeTruthy();
  });
});
